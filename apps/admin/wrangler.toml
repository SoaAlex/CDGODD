name = "cdgodd-admin"
compatibility_date = "2026-06-01"

routes = [
  { pattern = "admin.cestdegaucheoudedroite.fr", custom_domain = true },
]

# Assets-only Worker: serves the Vite build, no server code.
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
