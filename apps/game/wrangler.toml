name = "cdgodd-game"
compatibility_date = "2026-06-01"

routes = [
  { pattern = "cestdegaucheoudedroite.fr", custom_domain = true },
]

# Assets-only Worker: serves the Expo web export (expo export -p web).
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
