{
  "name": "@cdgodd/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit && tsc --noEmit -p test",
    "test": "vitest run",
    "e2e:server": "rm -rf .wrangler/e2e-state && wrangler d1 migrations apply cdgodd --local --persist-to .wrangler/e2e-state && wrangler d1 execute cdgodd --local --persist-to .wrangler/e2e-state --file=./src/db/seed.sql && wrangler dev --local --port 8789 --persist-to .wrangler/e2e-state --var ADMIN_TOKEN:cdgodd-e2e-token --var CDN_BASE:http://localhost:8789/img",
    "db:migrate:local": "wrangler d1 migrations apply cdgodd --local",
    "db:migrate:remote": "wrangler d1 migrations apply cdgodd --remote",
    "db:seed:local": "wrangler d1 execute cdgodd --local --file=./src/db/seed.sql"
  },
  "dependencies": {
    "@cdgodd/shared": "workspace:*",
    "hono": "^4.8.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.4",
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.22.0"
  }
}
